# Plan: Add Voice Design + Registration to Mobile (1:1)

## Context

Web's voice system:
- `voice-design.ts` (84 lines): VoiceDesign interface + prompt builder + normalizer
- `voice-registration.ts` (56 lines): Provider-neutral registration seam

Mobile has no voice design or registration.

## Implementation

### Step 1: Create voice design types and utilities
**New file**: `apps/expo/src/core/voice/voiceDesign.ts`

Port of Web's voice-design.ts:
- VoiceDesign interface (identity, texture, delivery)
- buildVoiceDesignPrompt()
- normalizeVoiceDesign()
- getDeterministicVoiceId()

### Step 2: Create voice registration interface
**New file**: `apps/expo/src/core/voice/voiceRegistration.ts`

Port of Web's voice-registration.ts:
- VoiceRegistrationAdapter interface
- Provider registry
- getVoiceRegistrationAdapter()

## Files

| Action | File | Purpose |
|--------|------|---------|
| **Create** | `apps/expo/src/core/voice/voiceDesign.ts` | Voice design utilities |
| **Create** | `apps/expo/src/core/voice/voiceRegistration.ts` | Registration interface |

## Verification

1. **Design test**: Create VoiceDesign → verify prompt builds correctly
2. **Normalization test**: Pass raw LLM output → verify normalizes
3. **ID test**: Same design → verify deterministic ID
