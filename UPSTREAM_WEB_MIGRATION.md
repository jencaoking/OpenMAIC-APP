# 上游 Web 版适配说明

本分支以 [THU-MAIC/OpenMAIC](https://github.com/THU-MAIC/OpenMAIC) 的最新主分支提交 `59adbef03b63488a7ff1ea983cf0fbba51609f9f` 为基线，对应发布版本 **v0.3.2**。它采用“上游优先”的安全策略：Web 运行时、Web UI、核心包、上游测试和官方持续集成均以官方最新实现为准，而不是在本项目旧快照上进行高风险冲突拼接。

> 当前项目来自上游 v0.3.0 的无共同提交历史快照，并在其上扩展了多个平台与存储能力。三方合并演练产生约 180 个冲突文件，因此本分支不尝试自动保留这些高耦合改动。

## 本分支包含的内容

| 范围 | 策略 | 结果 |
|---|---|---|
| Web 应用与 API | 使用上游 v0.3.2 | 获得官方最新功能、修复和测试覆盖。 |
| 核心包与 DSL | 使用上游 v0.3.2 | 避免将旧快照的类型、存储与导出契约混入新版。 |
| Web 测试与 CI | 使用上游 v0.3.2 | 使用上游维护的质量门禁和构建策略。 |
| 根目录部署配置 | 使用上游 v0.3.2 | 保持官方 Web 部署模型，减少隐性耦合。 |

## 有意隔离的当前项目定制

下列内容保留在原来的 `main` 分支，不会被删除，但不会自动带入本分支：

| 定制范围 | 原因 | 后续建议 |
|---|---|---|
| `apps/expo/` 与 `eas.json` | 上游 Web 主线没有 React Native/Expo 工程，且其 API、存储和 DSL 契约已发生变化。 | 在 Web 版稳定后，单独建立移动端兼容迁移项目。 |
| `electron/` | 上游未维护同等桌面封装入口，直接复制会隐藏运行时和打包兼容问题。 | 以后以新版 Web 构建产物为基线单独恢复。 |
| `packages/maic-storage-server/` 与 `Dockerfile.storage-server` | 上游的持久化与资产管理模型发生较大演进，旧服务不能被安全假定兼容。 | 先完成新旧数据契约映射，再独立迁移。 |
| 旧版 Web 定制接口、生成流程、导出和 UI 修改 | 与上游新增的生成、媒体、持久化、播放和导出改动存在交叉冲突。 | 按功能域逐项评审与迁移，并为每项添加回归测试。 |

## 验收边界

合并此分支的含义是“Web 版回归官方 v0.3.2”，而不是“全部现有功能无差别保留”。在合并前，应以官方提供的安装、类型检查、单元测试、构建和端到端验证为准；移动端、桌面端、旧存储服务及其数据兼容性不属于本次验收范围。

## 参考

[1]: https://github.com/THU-MAIC/OpenMAIC/tree/59adbef03b63488a7ff1ea983cf0fbba51609f9f "上游最新主分支提交"
[2]: https://github.com/THU-MAIC/OpenMAIC/releases/tag/v0.3.2 "OpenMAIC v0.3.2 发布说明"
[3]: https://github.com/THU-MAIC/OpenMAIC/releases/tag/v0.3.0 "OpenMAIC v0.3.0 发布说明"
