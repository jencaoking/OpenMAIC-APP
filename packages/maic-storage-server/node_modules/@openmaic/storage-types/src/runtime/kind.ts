/**
 * Core runtime kind names recognized by the contract.
 * @remarks This is a pure type contract. Zero runtime dependencies. Safe for Expo/Metro bundler.
 */
export type CoreRuntimeKind = 'chat' | 'quizAttempt' | 'playback';